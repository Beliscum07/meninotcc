import 'package:flutter/material.dart';

class LoginDoadorPage extends StatelessWidget {
  const LoginDoadorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Login Doador')),
      body: const Center(
        child: Text('Página de login do doador (placeholder)'),
      ),
    );
  }
}
