import 'package:flutter/material.dart';

class DoadorDoar extends StatelessWidget {
	const DoadorDoar({super.key});

	@override
	Widget build(BuildContext context) {
		return const SafeArea(
			child: Center(child: Text('Fazer Doação')),
		);
	}
}
