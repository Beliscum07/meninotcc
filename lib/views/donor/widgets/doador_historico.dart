import 'package:flutter/material.dart';

class DoadorHistorico extends StatelessWidget {
	const DoadorHistorico({super.key});

	@override
	Widget build(BuildContext context) {
		return const SafeArea(
			child: Center(child: Text('Minhas Doações')),
		);
	}
}
