import 'package:flutter/material.dart';

class StudentActivities extends StatelessWidget {
  const StudentActivities({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Atividades'));
  }
}
