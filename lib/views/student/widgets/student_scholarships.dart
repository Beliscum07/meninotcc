import 'package:flutter/material.dart';

class StudentScholarships extends StatelessWidget {
  const StudentScholarships({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Bolsas'));
  }
}
